## 5/7. 우선순위 큐

- ADT
- 우선순위 큐의 구현
- 트리의 구현 : 배열 or 연결된 표현법 - 종류에 따라
- 완전이진트리 -> 배열로 구현이 유리 / HEAP : 완전이진트리 - 노드에 번호 붙일 수 있고 이게 배열의 index와 맵핑
- Heap : 중복 키 허용. / 최대 Heap과 최소 Heap / 느슨한 형태의 정렬.

수업에서 다룰 건 최대 heap.

1번 노드 ~ 마지막 노드 사이 빈칸 X



#### 힙 정렬

대충 정렬 해놓은 상태에서 삭제연산 시 항상 O(1)의 최대 가중치를 끌어내고 그 다음 힙을 만들때 O(logN)의 시간복잡도

N번 delete 하면 총 시간복잡도는 O(NlogN)